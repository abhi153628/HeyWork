// /data/repositories/auth/auth_repository_impl.dart
// Purpose: Implements the auth repository interface from domain layer
// Uses auth service to perform authentication operations
// Handles errors and converts them to domain-specific failures